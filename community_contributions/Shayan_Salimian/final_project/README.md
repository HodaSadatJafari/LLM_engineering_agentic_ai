# 📱 فروشگاه موبایل هوشمند با پایتون و Gradio

این پروژه یک **فروشگاه موبایل هوشمند** است که با **پایتون** و **Gradio** ساخته شده و از **OpenAI GPT-4o-mini** برای چت با کاربر استفاده می‌کند.  
کاربر می‌تواند محصولات را مشاهده و جستجو کند، سفارش ثبت کند و با فروشنده‌ی هوشمند مکالمه داشته باشد.

---

## ⚡ ویژگی‌ها

1. **نمایش محصولات و جستجو**
   - تمام محصولات موجود نمایش داده می‌شوند
   - قابلیت جستجوی محصول بر اساس نام

2. **ثبت سفارش مستقیم**
   - فرم با **نام مشتری، شماره تماس و انتخاب محصول از Dropdown**
   - ذخیره سفارش در فایل `orders.json`
   - پیام موفقیت بعد از ثبت سفارش

3. **چت با هوش مصنوعی (AI)**
   - پاسخ به سوالات کاربر درباره محصولات
   - تشخیص قصد خرید کاربر
   - هدایت مراحل خرید و ثبت سفارش

4. **پایدار و سازگار**
   - مناسب Gradio نسخه 3.13 و Python 3.13  
   - بدون ارورهای نسخه‌ای

---

## 🛠️ پیش‌نیازها

1. Python 3.13  
2. نصب پکیج‌ها از `requirements.txt`:

```bash
pip install -r requirements.txt
