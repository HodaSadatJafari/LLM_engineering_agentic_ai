# 🛒 چت‌بات خدمات مشتریان فروشگاه آنلاین

سلام! 👋

این یک چت‌بات هوشمند برای فروشگاه‌های اینترنتی هست که با استفاده از هوش مصنوعی به مشتری‌ها کمک می‌کنه محصولات رو پیدا کنن، تخفیف‌ها رو ببینن و سفارش بدن. خیلی راحت و سریع!

## 💡 این چت‌بات چیکار می‌کنه؟

فکرشو بکن یه فروشنده خیلی باهوش داری که:
- 🔍 **جستجوی هوشمند**: وقتی می‌گی "یه موس گیمینگ می‌خوام"، دقیقاً چیزی که می‌خوای رو پیدا می‌کنه
- 💰 **نشون دادن تخفیف‌ها**: همه محصولات تخفیف‌خورده رو بهت معرفی می‌کنه
- 📦 **ثبت سفارش**: به صورت طبیعی بهش بگو چی می‌خوای، خودش سفارشت رو ثبت می‌کنه
- 💬 **حافظه مکالمه**: یادش می‌مونه قبلاً چی گفتی و مکالمه رو ادامه می‌ده
- 🎯 **متمرکز روی فروش**: فقط به سوالات مربوط به محصولات و سفارشات جواب می‌ده

## 🎬 چطوری کار می‌کنه؟

خیلی ساده! تو با چت‌بات حرف می‌زنی، اون از پایگاه داده محصولاتت جستجو می‌کنه و با استفاده از GPT-4 بهت جواب می‌ده. همین!

## 📋 چی لازم داری؟

- پایتون نسخه 3.8 یا بالاتر
- یه کلید API از OpenAI (یا سرویس‌های مشابه مثل Avalai)
- یه فایل JSON با محصولاتت

## 🚀 نصب و راه‌اندازی

### قدم اول: دانلود پروژه

```bash
git clone https://github.com/yourusername/ecommerce-chatbot.git
cd ecommerce-chatbot
```

### قدم دوم: نصب کتابخانه‌ها

```bash
pip install -r requirements.txt
```

خیلی راحت! همه چیزی که لازم داری نصب میشه.

### قدم سوم: تنظیم کلید API

یه فایل به اسم `.env` بساز و اطلاعاتت رو توش بنویس:

```env
# لازمه! کلید API خودت رو اینجا بنویس
OPENAI_API_KEY=کلید_API_خودت

# اگه از سرویس دیگه‌ای استفاده می‌کنی (مثلاً Avalai)
OPENAI_BASE_URL=https://api.avalai.ir/v1

# اختیاری - اگه نذاری، مقادیر پیش‌فرض استفاده میشه
MODEL_NAME=gpt-4o
EMBEDDING_MODEL=text-embedding-3-small
```

### قدم چهارم: دانلود و تنظیم فایل‌های محصولات

حالا باید دو تا فایل JSON رو دانلود کنی و آدرسشون رو توی کد بذاری:

#### 1️⃣ فایل‌ها رو دانلود کن:

**دو تا فایل نیاز داری:**
- 📦 **products.json** - لیست محصولات عادی فروشگاهت
- 💰 **sales.json** - لیست محصولات تخفیف‌دار


#### 2️⃣ فایل‌ها رو جایی ذخیره کن:

فایل‌ها رو توی یه جایی که راحت پیداشون می‌کنی ذخیره کن، مثلاً:
- `C:\Users\YourName\Desktop\products.json`
- `C:\Users\YourName\Desktop\sales.json`


#### 3️⃣ آدرس فایل‌ها رو توی کد بنویس:

فایل `app.py` رو باز کن و این قسمت رو پیدا کن (حدوداً خط ۲۸):

```python
# Data file paths (relative to project root)
PRODUCTS_DATA_PATH = Path("./data/products.json")
SALES_DATA_PATH = Path("./data/sales.json")
```

حالا آدرس فایل‌های خودت رو بنویس:

```python
# آدرس فایل‌های محصولات خودت رو اینجا بنویس
PRODUCTS_DATA_PATH = Path(r"C:\Users\YourName\Desktop\products.json")
SALES_DATA_PATH = Path(r"C:\Users\YourName\Desktop\sales.json")
```

**نکته مهم**: حتماً قبل از آدرس، حرف `r` رو بنویس تا مشکلی پیش نیاد!

#### 📝 فرمت فایل‌های 
## 🎯 حالا وقت تستش!

### اجرا کن:

```bash
streamlit run app.py
```

بعدش مرورگرت باز میشه و چت‌بات آماده‌ست! 🎉

### بار اول که اجرا می‌کنی:

چت‌بات یه کم طول می‌کشه چون داره:
1. فایل‌های JSON رو می‌خونه
2. برای همه محصولات embedding می‌سازه
3. پایگاه داده vector رو می‌سازه و ذخیره می‌کنه

این کار یه بار انجام میشه و دفعات بعد خیلی سریع لود میشه!

## 💬 چطوری استفاده کنم؟

خب، همین الان می‌تونی باهاش حرف بزنی! مثلاً:

```
تو: سلام! موس گیمینگ داری؟
بات: بله! محصولات زیر رو داریم: [لیست موس‌ها]

تو: چی تخفیف داری؟
بات: این محصولات الان تخفیف دارن: [لیست تخفیف‌ها]

تو: موس بی‌سیم رو می‌خوام. اسمم علی رضایی‌ه، 
     ایمیلم ali@example.com و شماره‌م ۰۹۱۲۳۴۵۶۷۸۹
بات: سفارش شما برای علی رضایی ثبت شد!
```

## 📁 ساختار پروژه

```
ecommerce-chatbot/
├── app.py                  # برنامه اصلی
├── requirements.txt        # کتابخانه‌های مورد نیاز
├── .env                    # تنظیماتت (این رو خودت بساز)
├── .env.example           # نمونه فایل تنظیمات
├── README.md              # همین فایلی که داری می‌خونی!
├── data/                  # محصولاتت
│   ├── products.json
│   └── sales.json
└── chroma_db/            # پایگاه داده (خودش ساخته میشه)
    ├── products/
    └── sales/
```

## 🔧 تنظیمات بیشتر

### می‌خوای مدل رو عوض کنی؟

توی فایل `.env` اینو بنویس:

```env
MODEL_NAME=gpt-3.5-turbo  # ارزون‌تر و سریع‌تر
# یا
MODEL_NAME=gpt-4o         # باکیفیت‌تر ولی کمی گرون‌تر
```

### می‌خوای نتایج جستجو بیشتر بشه؟

توی `app.py` این خط رو پیدا کن و عدد رو عوض کن:

```python
retriever_products = db_products.as_retriever(search_kwargs={"k": 10})  # به جای 5، حالا 10 تا نتیجه میده
```

### می‌خوای رفتار چت‌بات رو شخصی‌سازی کنی؟

توی `app.py` دنبال قسمت `system prompt` بگرد و متن راهنمایی رو بنویس. مثلاً بگو چطوری حرف بزنه یا به چی جواب نده.

## 🐛 مشکلات رایج و راه‌حل‌ها

### پایگاه داده خالیه! (0 items)

**راه‌حل**: پوشه `chroma_db` رو پاک کن و دوباره اجرا کن:

```bash
rm -rf chroma_db/
streamlit run app.py
```

### محصولی پیدا نمی‌کنه!

**راه‌حل**: فایل JSON رو چک کن:
- مطمئن شو که کلید اصلی `"products"` یا `"sales"` هست
- هر محصول باید `name`, `price`, `rating`, `description` داشته باشه
- نظرات باید توی لیست باشن مثل `["نظر اول", "نظر دوم"]`

### خطای کلید API

**راه‌حل**: فایل `.env` رو چک کن. مطمئن شو که:
- توی همون پوشه پروژه هست
- کلید API درست وارد شده
- هیچ فضای اضافی یا enter اضافی نداره

## 🔐 نکات امنیتی

- **هیچ‌وقت فایل `.env` رو commit نکن!** (همیشه توی `.gitignore` هست)
- کلید API خودت رو با کسی شریک نکن
- اگه می‌خوای عمومی استفاده بشه، حتماً rate limiting بذار

## 📦 کتابخانه‌هایی که استفاده شده

- `streamlit` - رابط کاربری وب
- `langchain` - فریمورک agent
- `langchain-openai` - اتصال به OpenAI
- `langchain-chroma` - اتصال به ChromaDB
- `chromadb` - پایگاه داده vector
- `python-dotenv` - مدیریت متغیرهای محیطی
- `pydantic` - اعتبارسنجی داده‌ها

